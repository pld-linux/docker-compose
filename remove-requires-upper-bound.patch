# Remove dependency version constraints not relevant in Fedora/EPEL
--- docker-compose-1.6.2/setup.py.orig	2016-02-12 16:42:36.000000000 +0200
+++ docker-compose-1.6.2/setup.py	2016-05-02 22:34:41.740785187 +0300
@@ -28,16 +28,16 @@
 
 
 install_requires = [
-    'cached-property >= 1.2.0, < 2',
-    'docopt >= 0.6.1, < 0.7',
-    'PyYAML >= 3.10, < 4',
-    'requests >= 2.6.1, < 2.8',
-    'texttable >= 0.8.1, < 0.9',
-    'websocket-client >= 0.32.0, < 1.0',
+    'cached-property',
+    'docopt',
+    'PyYAML',
+    'requests',
+    'texttable',
+    'websocket-client',
     'docker-py > 1.7.2, < 2',
     'dockerpty >= 0.4.1, < 0.5',
-    'six >= 1.3.0, < 2',
-    'jsonschema >= 2.5.1, < 3',
+    'six',
+    'jsonschema',
 ]
 
 
@@ -47,8 +47,8 @@
 
 
 if sys.version_info[:2] < (3, 4):
-    tests_require.append('mock >= 1.0.1')
-    install_requires.append('enum34 >= 1.0.4, < 2')
+    tests_require.append('mock')
+    install_requires.append('enum34')
 
 
 setup(
